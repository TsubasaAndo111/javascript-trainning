## localStorageとsessionStorageの有効期限の違い

### localStorage

データは明示的に削除されるまで永続する。

ブラウザを閉じても残る

有効期限なし

### sessionStorage

データはタブ（ブラウザウィンドウ）のセッションが続く間だけ保持。

タブを閉じると消える。

リロードでは消えない（同じタブの場合）

有効期限はセッション終了時
